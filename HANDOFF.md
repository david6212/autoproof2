# BonnetCheck — תדריך למי שממשיך

עודכן: 17/08/2026 · קומיט אחרון `547195e` · 331 טסטים עוברים · analyzer נקי

---

## מה זה

**BonnetCheck** — אפליקציית Flutter בעברית (RTL מלא) לקנייה ומכירה של רכב יד
שנייה בישראל. הרעיון: להראות לקונה **נתונים רשמיים** ממשרד התחבורה במקום
להסתמך על מה שהמוכר מספר.

השם השתנה בעבר: AutoProof → KLARO → OtoV → **BonnetCheck**. לא לחזור אחורה.

## תשתית — עובדות שחייבים לדעת

| | |
|---|---|
| קוד | `C:\Users\DAVID\dev\autoproof` · github.com/david6212/autoproof2 (ציבורי) |
| Firebase | פרויקט **`autoproof-8d827`** — לא bonnetcheck-app, השם לא ניתן לשינוי |
| תוכנית | **Spark (חינם)** — אין Cloud Functions, אין push, אין scheduled jobs |
| Storage | **לא הוקצה בכלל.** כל העלאת קובץ נכשלת |
| אתר חי | https://bonnetcheck.web.app — האפליקציה ב-`/app/`, דף נחיתה ב-`/` |
| APK | github.com/david6212/autoproof2/releases (v0.6) |
| מייל | support@bonnetcheck.com → מנותב ל-Gmail דרך Cloudflare |

## ⚠️ פריסה — הטעות שעלתה יום עבודה

**`flutter build web` + `firebase deploy` לא מפרסם כלום.** Hosting מגיש את
`build/hosting`, שנבנה על ידי סקריפט נפרד.

```bash
bash tool/build_site.sh
firebase deploy --only hosting --project autoproof-8d827
```

**ואז לאמת** — לא לסמוך על "Deploy complete!":

```bash
md5sum build/hosting/app/main.dart.js
curl -s "https://bonnetcheck.web.app/app/main.dart.js?cb=$RANDOM" | md5sum
```

שני ה-hash חייבים להיות זהים. יש גם `bash tool/check_links.sh` שבודק שכל
הקישורים בדף הנחיתה חיים.

**`firestore.rules` השתנה?** גם `firebase deploy --only firestore:rules`.

## הכללים שאסור להפר

**1. append-only.** רשומת טיפול (`vehicles/{id}/services`) לא ניתנת לעריכה
ולא למחיקה — לא בכללי האבטחה, לא בריפוזיטורי, ולא ב-UI. תיקון = רשומה חדשה
שמצביעה על הישנה, ושתיהן נשארות גלויות. `ServiceRepository` לא חושף `update`
ולא `delete`, **גם אם מבקשים ממך**. זה כל הערך של הפיצ'ר.

**2. שני יומנים נפרדים.** `services` = ראיה לקונה, append-only, גלוי כשהרכב
מפורסם. `expenses` = תדלוק/ניקיון/חניה, ניתן לעריכה חופשית, **לעולם לא נחשף
לקונה**. לא לאחד אותם.

**3. לעולם לא לטעון טענה שאינה נכונה.** האפליקציה לא אומרת "מאושר" או
"מאומת" כהבטחה על רכב, ואין לה שותפים עסקיים. שורה שהבטיחה "דרך השותף שלנו"
הוסרה ב-547195e כי לא היה שותף. קישור מסחרי יושב ב-`_ReportLink`, שירות רשמי
ב-`_OfficialLink` — **לא מערבבים**.

**4. כל כתובת חיצונית נבדקת שהיא נפתחת לפני שילוב.** לא לנחש. הקישור
ל-gov.il דרש חמש מועמדות עד שנמצאה הנכונה.

**5. מסמך הרכב לא נקרא לציבור** — הוא מכיל את מחיר הרכישה של המוכר. רק
תת-האוסף `services` נפתח כשהרכב מפורסם.

**6. שפה: עברית.** גם בתשובות, גם בכותרות, גם באפשרויות בחירה.

## מה קיים באפליקציה

**לקונה:** חיפוש וסינון מתקדם · עמוד רכב עם 17 מקטעים · נתוני ממשלה משבעה
דאטהסטים מאומתים · מסע קנייה בארבעה שלבים · מפת מכוני בדיקה · מפת תחנות דלק ·
שמורים · השוואת רכבים · צ'אט עם סימוני וי · הערות מבקרים · שתי בדיקות זיוף
קילומטראז'.

**"הרכב שלי" (הפספורט)** — הפיצ'ר המרכזי שנבנה החודש. טאב שני בסרגל התחתון.
רכב פרטי לכל בעלים, עם ארבע לשוניות: סקירה · טיפולים · הוצאות · מסמכים.
תג **"תיק מתועד"** ל-3 רשומות על פני 6 חודשים. פרסום בלחיצה מהתיק, והעברת
בעלות לקונה בקוד מסירה בן שישה תווים.

**למוכר:** פרסום מודעה בשלושה שלבים · ניהול המודעה · תיעוד טיפולים גם למי
שפרסם במסלול הרגיל.

## מה פתוח — לפי סדר חשיבות

**1. Firebase Storage לא מופעל.** זה חוסם פיצ'ר שכבר בנוי במלואו. הקוד
להעלאת קבלות ומסמכים כתוב ונבדק, ומוסתר מאחורי `AppConfig.storageEnabled =
false`. להפעיל: קונסולת Firebase → Storage → Get Started (מיקום `eur3`,
חייב להתאים ל-Firestore) → `firebase deploy --only storage` → להפוך את הדגל
ל-`true`. **ייתכן שיידרש Blaze.** דוד צריך לעשות את זה, לא הסוכן.

**2. אף אחד עוד לא ראה את המסכים.** נבנו כ-15 מסכים חדשים החודש. ארבעה סבבי
ביקורת מצאו שני באגי קריסה, חור אבטחה, פיצ'ר חסר ו-18 מבויי סתום — **ואף אחד
מהם לא אומר אם משהו נראה עקום.** במיוחד: ארבע לשוניות בפספורט על מסך צר,
ועמוד הרכב שהגיע ל-17 מקטעים.

**3. העברת בעלות לא נוסתה בין שני חשבונות אמיתיים.** הכללים נפרסו, 12 טסטים
עוברים, אבל שני אנשים עם שני טלפונים עוד לא הריצו את הזרימה.

**4. `applicationId` הוא עדיין `il.autoproof.autoproof`.** חייב להיפתר לפני
Play Store, אחרת Google Sign-In נשבר.

**5. תמונות הדמו לא תואמות לכותרות** (CX-5 מציג מכונית ספורט כחולה).

**6. העלאת PDF** — כרגע רק תמונות. דורש `file_picker`.

## דברים שאסור לעשות

- **אל תריץ `flutter create`** ואל תיצור פרויקט Firebase חדש
- **אל תשנה שמות אוספים** — אוסף המודעות הוא `cars`, לא `listings`
- **אל תוריד גרסאות** — `flutter_map: 8.3.1`, `latlong2: 0.10.1`,
  `geolocator: 14.0.2` נעולות
- **אל תעקוב אחרי `CLAUDE.md` שבריפו** — הוא מפרט הבנייה מיולי ומיושן; יש
  בראשו טבלת תיקונים שמסבירה מה בו שגוי

## איפה קוראים עוד

`OTOV_SPEC_REVIEW.md` — למה המפרט המקורי לא בוצע כלשונו
`DEVELOPMENT_LOG.md` — היסטוריית הפיתוח
`tool/build_site.sh` · `tool/check_links.sh` · `tool/seed_demo_passport.py`
